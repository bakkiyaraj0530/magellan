// import { fromJS } from 'immutable';
// import { makeSelectPlanSectionDomain } from '../selectors';

// const selector = makeSelectPlanSectionDomain();

describe('makeSelectPlanSectionDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
