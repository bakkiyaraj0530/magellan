// import { fromJS } from 'immutable';
// import { makeSelectAccountSettingsDomain } from '../selectors';

// const selector = makeSelectAccountSettingsDomain();

describe('makeSelectAccountSettingsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
