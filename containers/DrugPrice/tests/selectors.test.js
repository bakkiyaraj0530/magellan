// import { fromJS } from 'immutable';
// import { makeSelectDrugPriceDomain } from '../selectors';

// const selector = makeSelectDrugPriceDomain();

describe('makeSelectDrugPriceDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
