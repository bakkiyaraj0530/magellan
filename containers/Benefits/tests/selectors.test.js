// import { fromJS } from 'immutable';
// import { makeSelectBenefitsDomain } from '../selectors';

// const selector = makeSelectBenefitsDomain();

describe('makeSelectBenefitsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
