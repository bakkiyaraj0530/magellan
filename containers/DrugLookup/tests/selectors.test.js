// import { fromJS } from 'immutable';
// import { makeSelectDrugLookupDomain } from '../selectors';

// const selector = makeSelectDrugLookupDomain();

describe('makeSelectDrugLookupDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
