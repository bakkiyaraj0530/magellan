// import React from 'react';
// import { shallow } from 'enzyme';

// import IndicatesField from '../index';

describe('<IndicatesField />', () => {
  it('Expect to have unit tests specified', () => {
    expect(false).toEqual(false);
  });
});
